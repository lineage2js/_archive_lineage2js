# lineage 2 js

node.js emulator server 

server for lineage 2 Chronicle 1 Harbingers of War

config:
data/config.json

npm:
npm install -i

### run on linux:

node loginserver.js

node gamaserver.js

### run on window:

serverStart.bat
